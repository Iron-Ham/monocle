// By Dennis Müller

import Foundation

/// Shared version identifier for the monocle tool and its LSP client info.
public enum MonocleVersion {
  /// Current semantic version string for the monocle tool.
  public static let current = "1.0.2"
}
